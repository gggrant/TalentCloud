{# =============================================================================

    Talent Cloud
    Applicant Profile - Skills

============================================================================= #}

{% extends "layouts/base" %}

{% block title %}{{ profile.reference_section.reference_title }}{% endblock %}

{% block body %}

	{% include "common/alert" %}

    {% include "common/goc" %}

    {% include "common/header" with {"header":profile} %}

    <a id="skipLink"></a>

    {# Menu #}
    {% include "applicant/profile/menu" %}

    {# References #}
    <section class="applicant-profile__portfolio">
        <div class="container--layout">
            <reference-list
                :lang="{{ trans('applicant/profile_references')|json_encode() }}"
                :lang-reference="{{ trans('common/references')|json_encode() }}"
                :references="{{ applicant.references|json_encode() }}">
            </reference-list>
        </div>
    </section>
    {# {% include "applicant/profile/references/index" %} #}

    {# Modals #}
    {% include "common/modal" with {"page":profile} %}

{% endblock %}
